/**
 * 全科補習班自訂 JavaScript
 */

(function ($, Drupal) {
  'use strict';

  Drupal.behaviors.tutoringMahi = {
    attach: function (context, settings) {
      // 在這裡添加你的自訂 JavaScript 代碼
      // 例如：
      // $('body', context).once('tutoringMahi').each(function () {
      //   console.log('全科補習班主題已載入');
      // });
    }
  };

})(jQuery, Drupal);
